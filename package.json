{
  "name": "gutu-lib-ui",
  "private": true,
  "type": "module",
  "workspaces": [
    "framework/libraries/*"
  ],
  "scripts": {
    "build": "bun run --cwd framework/libraries/ui build",
    "typecheck": "bun run --cwd framework/libraries/ui typecheck",
    "lint": "bun run --cwd framework/libraries/ui lint",
    "test": "bun run --cwd framework/libraries/ui test",
    "test:unit": "bun run --cwd framework/libraries/ui test:unit",
    "docs:summary": "node scripts/docs-summary.mjs",
    "docs:check": "node scripts/docs-check.mjs"
  }
}
